"""Trust layer: make the label on a number come from the code that made the number.

`from trust.provenance import Run` works when the repo root is on sys.path (it is, for
scripts run as `python analysis/.../x.py` from the repo root).
"""
