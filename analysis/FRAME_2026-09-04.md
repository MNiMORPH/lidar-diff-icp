# FRAME 2026-09-04 — read this FIRST after compaction, before acting

Verify every structural claim here against git / the files before relying on it. This is a
map of ROLES and NEXT ACTIONS, not a results log.

## Where the work is

The vegetation correction for gen2's ground is now ONE implementation, wired into both the
tile producer and the pipeline. **The build is finished. The correction itself is now in
question**, on measurement, at BOTH sites — see "The result that stops us" below.

## The method — one object, two halves, in `src/lidar_diff_icp/groundq.py`

`analysis/GROUND_Q_FROM_CLASS2_SPREAD.md` is the authority on the derivation. The code:

| half | functions |
|---|---|
| calibrate | `mark_statistics` · `spatial_folds` · `fit_curve` · `save_curve` |
| apply | `surface_from_grid` · `reference_surface` · `column_histogram` · `spread_from_histogram` · `ground_at_q` · `ground_at_median` · `correct_gen2` |

They must measure the SAME statistic — the plain SD of the ground-class column about the
local surface — or the curve is read at the wrong place on its own x-axis.
`tests/test_groundq.py` pins that, and pins that the robust spread which was briefly used
instead disagrees by more than the calibrated range is wide.

    rank of surveyed ground within class-2 returns: median 0.4532, class-2 median +8.1 mm high
    curve: q = 0.506 at 20-40 mm, 0.445 at 60, 0.395 at 120, 0.237 at 200, 0.101 at 400 mm
    held out, 10 km spatially blocked: +8.1 mm bias -> -2.4, RMS 124.5 -> 104.6 mm

Reproduce every number, and the curve byte-identically:

    ./lidar-icp/bin/python analysis/calibrate_ground_q.py --set gen2_2021_control --diagnostics

## Callers — there is no second implementation

- `analysis/ridgelines/dod_cover_corrected.py --q-from-class2-spread` — the TILE route.
  Verified byte-identical across the lift on all four Elba arrays.
- `pipeline.difference_dem(ground_q="calibrated")` — applies it as a post-registration
  DELTA, `h2(calibrated) - h2(median)`, so (1) the gen1 tie is fitted against the
  UNCORRECTED surface and cannot absorb the correction, and (2) the frame cancels exactly
  rather than smuggling in the low_q-vs-slope_normal difference. Second pass by necessity:
  the reference surface IS the q = 0.50 grid.
- `_stream_ground` is back to a plain quantile. The retrofit is deleted.

## THE RESULT THAT STOPS US

The agreed gate was: Whitewater's `stable_sigma` must FALL below 86.3 mm. It did not.

    stable_1sigma_m   uncorrected 0.0863   corrected 0.0872

On the 241,747 cells BOTH runs produced — the like-for-like comparison, since the corrected
run declines 41,262 cells:

    uncorrected  NMAD  85.01 mm   sd 202.62   median -11.75
    corrected    NMAD  92.35 mm   sd 218.76   median -22.29

And the same test applied to ELBA, which nobody had run — we had only ever checked the
median there:

    ELBA, same 340,020 cells
      gen2 at its median    NMAD 74.77 mm   median +6.10
      gen2 at calibrated q  NMAD 79.09 mm   median -3.33

**The correction improves the LEVEL and worsens the SCATTER, at both sites.** Whitewater is
not anomalous; Elba behaves the same way and its success was median-only.

HYPOTHESIS, not measured: held-out error at the marks is 104.6 mm RMS, larger than the DoD's
own ~75 mm NMAD, so applying the curve per cell may inject more noise than it removes.
Reducing error against surveyed truth and reducing DoD dispersion are not the same objective.
Testable by applying the curve's PREDICTED LEVEL only (a smooth function of cover) instead of
its per-cell value. NOT run. Andy's call.

## Products, and where each state lives

- **Elba** `data/derived/elba/` — corrected via the tile route, WITH the NaN fix: 340,020
  cells. Base `dod.npy` is NOT corrected.
- **Whitewater** `data/derived/whitewater/` — RESTORED to uncorrected (`ground_percentile`
  0.5, `stable_1sigma_m` 0.0863), because the corrected run failed its gate. The corrected
  outputs are preserved in the session scratchpad under `ww_corrected_run/`, and the
  original snapshot under `ww_base_snapshot/`.
- No other site has been touched by any of this.

## Guardrails that still hold

- **No windows, no cutoffs, no classification.**
- **Per-epoch always.** `load_curve` refuses on the epoch recorded INSIDE the file. gen1's
  calibration was run and FAILED (flat at 0.219, q = 0.50 biased +89.8 mm) — a statement
  about the 2008 control's uncertain vertical reference, not the lidar. **Andy: leave it be.**
- **Refuse, never fall back.** Missing curve, epoch mismatch, in-memory path — all raise.
- **A declined cell must not look corrected.** `ground_at_q` used to read a NaN percentile as
  bin 0 and hand back the FLOOR of the column: 1,131 Elba cells carried a median DoD of
  -1.091 m of fabricated erosion where their plain median said +0.003 m. Fixed, with a
  regression test that fails without the fix.
- Shared laptop: one heavy job at a time, watch `free -h`.
- Commit granularly; never push/tag/release without explicit current-message authorization.
- Numbers in prose are pasted from command output, never typed.

## Negative results — do not re-walk these

- **Cover metrics are worse than the return column itself.** canopy cover rho +0.147,
  `lowveg` +0.175, against `mode_minus_p10` +0.385 and `mode_minus_median` +0.406.
- **Intensity** carries a real but small signal not separable from acquisition effects.
- **The mixture / scale-separation route is dead.** `sigma_g` is 51.6 mm on the BAREST marks.
- **Under heavy cover `w_g` = 0.000.** Not recoverable by any percentile.
- **The open-ground POWER-LAW tail is a pooling artifact.**
- **Folding the correction into `_stream_ground` fails**, and the reason is the frame: that
  function takes its residual against the REGIONAL plane on one call and against nothing at
  all on the other, while the curve is calibrated against a LOCAL fitted surface.

## Blocked, for when the correction is settled

`battlecreek` is configured `stream=False` in `scripts/run_all_sites.py` and
`ground_q="calibrated"` refuses on the in-memory path, so a six-site run raises there.
Changing it to `stream=True` is a decision, not a fix — not taken.

## State to verify on resume

    git log --oneline -12
    ./lidar-icp/bin/python -m pytest -q          # expect 332 passed
    git status --porcelain
