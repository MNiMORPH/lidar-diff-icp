# ★★ FRAME — 2026-08-26 PM. READ FIRST after compaction.

Verify every structural claim here against git/files before acting. Branch
`datum-fix-hillslope`, **PUSHED to origin 2026-08-26** — `git status -sb` showed
`datum-fix-hillslope...origin/datum-fix-hillslope`, 0 ahead. Andy pushed it himself.
Anything committed AFTER that point is local again; re-check with `git status` before
acting, and never assume the frame's snapshot is still true.
Pushing, tagging, releasing and closing issues all require Andy's explicit say-so.

## How we are working now — read before answering anything

This changed late on 2026-08-26 and it is the reason the work started going well. It is not
a mood; each item is a specific correction Andy had to make, and the trust gate in
`.claude/settings.local.json` enforces the first two mechanically.

- **Numbers are pasted, never typed** — verbatim from a command run this session, including
  numbers from subagents. Relaying a subagent's statistic as my own is the same error as
  inventing one. If it cannot be re-derived, it goes on a line starting `UNVERIFIED:` naming
  the source and what statistic it is.
- **Never fill a gap silently.** No minimum counts, thresholds, filters or defaults of my own.
  If a cut seems needed, say so in one sentence and let Andy decide. Asking costs him ~30 s;
  a silent threshold has cost him hours, twice.
- **Name the statistic, not just the value.** "SE" is not a quantity; "SE of the mean of the
  block medians, 3.54 mm" is.
- **Measurement and interpretation are separate acts.** Deliver the number bare. An
  interpretation emitted with its measurement has not been tested, and it then gets defended
  and used to pick the next experiment.
- **Answer the question asked, and stop.** No extra columns, no stacked caveats, no menu of
  next steps. Volume is what hides the one thing that is wrong.
- **Label by construction, never by ownership** — not "mine" or "pipeline" or "new", but which
  epoch, which ground source, which corrections, which quantile.
- **A pooled cross-cell comparison cannot separate a beam effect from a cell-selection
  effect.** Run the within-cell or within-swath version first.
- **When the gate fires, fix it in the turn.** Do not explain it away. It has been right both
  times.

If this slips, the escalation is `TRUST_LEVEL: normal` in `.claude/settings.local.json`, which
adds the interpretation-with-measurement rule and volume caps (measured: fires on 14% of
replies against core's 4%). `TRUST_OFF=1` is Andy's kill switch; I have no in-band override.

## Why this work exists

Bring gen1 (2008 MN DNR, leaf-off Nov) into gen2's (2021 3DEP, leaf-on May) frame so the DoD
stops reading false aggradation on forested hillslopes. Long game: a deterministic, reusable
per-swath correction for gen1 statewide. Elba / elbaext are the pilot tiles.

## NEXT ACTION

**Superseded 2026-08-26 by Andy's decision below: adopt the extent-invariant ties, do NOT
chase the tilt.** The former next action (fit the DoD plane within one swath to separate dE
from the across-swath constants) is retained only as history in
`analysis/TILT_NEXT_SESSION.md`; the tilt was refuted twice — see "The tilt is CLOSED" below.

## ★★ 2026-08-26 EVENING: gen1's absolute level, from its OWN 2008 control

**Elba's offset: +53.6 ± 13.0 mm — the constant to ADD to gen1 after swath alignment.**
18 open/urban marks on Elba's own flight lines (133-138), Elba's swath constants applied to
put them in one frame, SE over marks. Per-line averaging of the same marks gives
+55.0 ± 16.6 (SE over 6 lines); the two agree to 1.4 mm. Sign convention throughout:
`tie = surveyed − z_lidar`, positive = gen1 reads LOW.

Method, and what is deliberately absent: vendor class-2 ground (not CSF); **no geoid term**
(2008 control and raw gen1 are both NAVD88/GEOID03 — it cancels); **no chain** (every mark is
on an Elba line); **no Nuth-Kääb lateral shift** (that is a gen1→gen2 term and gen2 is not in
this comparison). Producer: scratchpad `screen_marks.py`, results `screen_results.csv`.

### The finding that changed the answer: the scatter is PER FLIGHT LINE

ANOVA over all 43 screened marks: **F = 8.63, p < 0.001**. Line means span −146.6 (line 149)
to +113.5 (line 134); sd between Elba's six line means is 40.7 mm. Marks therefore cannot be
pooled as independent draws from one constant, and **marks on other lines say nothing direct
about Elba**.

**This RETRACTS the earlier "the two instruments point opposite ways" framing.** The −28.6 mm
that appeared to contradict the +22.7 anchor came from four open marks of which THREE sat on
line 145 — not an Elba line. Sorted by line, the control and the anchor agree: +53.6 ± 13.0
against +22.7 ± 39.7 is 0.8σ.

### Negative result: the siting screen does not help

σ_site is **83–97 mm at every ladder cut** from 15 mm to no cut at all. Whatever limits a
single mark, it is not radius instability. My earlier claim that "~10 well-sited marks gets
±10 mm" was wrong; the limit is per-line coverage, not per-mark quality.

### First independent check on the swath network

Applying Elba's per-swath constants explains **31%** of the between-line sum of squares the
marks see. Residual line structure F(5,12) = 1.46, p = 0.2724 — which with three lines
carrying 1–2 marks is NOT evidence the constants are right, only that we cannot tell.

### Two flaws in the above, both known, both being worked

1. **The constants were fitted at Elba and applied out to 62.9 km.** `CROSS_LINE_FIT.md` showed
   the across-track coefficient is local to its overlap; the same may hold for the constant.
   No along-line drift is detectable (slope +0.17 ± 0.62 mm/km, p = 0.789) but the residual sd
   is 28.2 mm inside 10 km against 53–74 mm beyond — F(10,3) = 4.22, p = 0.263, Levene
   p = 0.239. Suggestive, unresolved.
2. **Line assignment used distance to the centreline**, a proxy, at ~968 m line spacing.
   It should come from the `point_source_id` of the returns at the mark.

### The 2021 anchors are NOT contemporaneous with gen1

2210 and 2036 were surveyed **2021-09-11**; zero 2008 control points lie within 100 m of any
2021 point (nearest: 1.58 km, 1.28 km). So the +22.7 anchor tie carries 13 years of real
ground change at a road shoulder. The 2008 control does not. See
[[anchors-not-contemporaneous]].

### RECOVERED prior art: `balancing_offset` already exists and was already run

`~/projects/catchment-dod-balance`, run on Elba 2026-08-20. From its `docs/validation.md`:

    | `keep`   | 1.0    | 0.999  | 0.99   | 0.95   | 0.90   |
    | datum (m)| −7.369 | −0.729 | −0.104 | −0.017 | −0.000 |

`keep = 1.0` is blunder-bound (one cell holding +7.494 m against a 99.9th percentile of 1.4 m)
and was correctly dismissed at the time. **But `keep = 0.99` gives −0.104 m — raise gen1 by
104 mm — the same sign and order as the control result, and it went unfollowed.** The lesson
Andy drew: "sensitive to a parameter nobody has justified" is a reason to justify the
parameter, not to set the result aside. The whole curve predates the geoid datum, the canopy
correction, the floodplain mask and the swath-tie rebuild, so it must be re-run.

### Data now on disk

42 gen1 tiles, 925 MB, under `data/before/`. Inventory of all 1,004 control points with tile
and cover class: scratchpad `control_inventory.csv` (1,004 marks span **547 tiles** — roughly
one mark per tile, which sets the cost of any enlargement).

## ★ DONE 2026-08-26 PM: the tie is ADOPTED and both tiles are REBUILT

Commits `9143203` (pipeline default `swath_tie="intercept"`, recorded in `corrections.json`)
and `9bb81cb` (one shared geoid plane for both tiles). Pre-tie products backed up under the
session scratchpad `pre_tie_backup/`. Rebuilt, in order: elba `dod`/`lod`/`corrections`
(47 s) → elbaext `dod_geoid`/`lod_geoid`/`stable_geoid`/`corrections_geoid` → both
`beam_offset_table.parquet` → elba `dod_cover_q2`. Peak RAM ~17 GB used / 30 GB available,
swap flat at 6 MB throughout.

**What it bought, measured on the PRODUCTS, not the constants.** Constants alone behaved
exactly as `SWATH_TIE_INTERCEPT.md` predicted — tile-to-tile disagreement, re-referenced to
swath 135, `136 −8.00 → −2.00`, `137 −9.80 → −2.10`, `138 −17.40 → −9.80`, RMS 12.42 → 5.90 mm.
The delivered DoDs did not follow:

| elba − elbaext DoD, shared 2540×3500 m | n | median | mean | NMAD | RMS |
|---|---|---|---|---|---|
| OLD (`overlap_median`) | 338,354 | +3.13 | +3.88 | 47.56 | 150.71 |
| NEW (`intercept`) | 337,781 | +2.71 | +3.22 | 47.69 | 151.61 |

0.42 mm on the median; NMAD and RMS flat. Within elba the DoD moved **−0.49 mm**, not the
predicted −5.76: the along-track drift re-absorbed the constants (drift means fell 4.7–5.4 mm
as the constants rose 5.1–5.9 mm). `dod_cover_q2` moved −0.52 mm median, ptp 6.9 mm; the
canopy term is unaffected (forest>0.35 minus open<0.05 on the stable population, +13.44 →
+13.16 mm — NOTE these are the project's `STRATA` cuts, NOT the definition behind the older
"+23.4 → −1.1 mm" headline; the two are not comparable and have not been reconciled).

**So: adopt it for correctness, not for accuracy.** The constants now estimate a stated
geometric position extent-invariantly, which matters when the same flight line is cut by a
different tile — the statewide case. It is NOT a product improvement and must not be quoted
as one. My earlier ranking of it first was wrong on that point.

**What this exposed and did not fix:** the along-track drift is fit per swath over each
tile's own footprint, so it is extent-dependent by construction, and it is the larger term.
A per-swath "constant + drift" comparison re-referenced to swath 135 appeared to show a
23.7 → 28.0 mm RMS worsening; that comparison is MIS-SPECIFIED (swath 135's own drift differs
between tiles, injecting that difference into every other swath) and should not be repeated
in that form. The DoD table above is the object that matters. The drift's extent dependence
is unmeasured and is the natural successor to this work.

**Left stale on purpose:** elbaext has no `dod_cover_q2`, and `dod_cover_corrected.py` reads
`{tile}/corrections.json` — which for elbaext is the OBSOLETE `reference_plane` product, not
`corrections_geoid.json`. Running it on elbaext as-is would silently use the wrong datum.
Fix the file-selection before extending the canopy correction to elbaext.

## ★ DECISION: use the extent-invariant ties. STOP chasing the tilt.

Andy, at session end: *"We can use these ties. No tilt chasing."*

### The ties — built, tested, ready to adopt (`analysis/SWATH_TIE_INTERCEPT.md`)

`coreg.across_track_tie` plus a `tie=` argument on `coregister_swaths`/`align_swaths`,
defaulting to the shipped `"overlap_median"` so **no existing behaviour changed** and elba's
constants still reproduce `corrections.json`. The tie is the **LAD (median-regression)
intercept at `tan θ_ref = tan θ_src`**, which reduces exactly to the shipped median when the
across-track ramp is zero. Regression test proven to bite: remove the fix and the two extents
disagree by 21.4 mm.

Elba/elbaext disagreement on shared swaths, before → after:

| swath | before | after | removed |
|---|---|---|---|
| 136 | −8.02 | **−2.04** | 75% |
| 137 | −9.66 | **−2.11** | 78% |
| 138 | −17.41 | **−9.81** | 44% |

RMS over the three lines excluding 138: 4.22 → **0.98 mm**. Only the two edge-cut links move
materially (elba 135‑136 −23.90 → −18.03; elbaext 133‑134 +18.88 → +21.99); every interior
link moves < 1.7 mm. **Swath 138's −9.81 mm survives and is now the only thing separating the
two tiles.**

**Integration when we choose to take it:** `tie="intercept"` at `pipeline.py:670`, record
`swath_tie` in `corrections.json`. It forces rebuilding every `dod`/`lod`/`z_before` and both
beam tables (elba's DoD moves −5.76 mm); old products stay reproducible on the default.
**Do NOT bundle the re-gauge.** Swath 137 is the symmetric choice (|mean scan| 0.58/0.55°
against 11.79/12.14° for the currently-pinned lines), but re-gauging raises gen1 by 32.5 mm
and takes the stable divide mean from −4.74 to −36.71 mm. That is a LEVEL decision belonging
with the absolute datum, where the divides and the surveyed marks point opposite ways.

### The tilt is CLOSED — do not re-open it

Refuted twice, by different methods. `STABLE_POINT_TILT_AUDIT.md`: dE is the valley limb
(−84.8 ± 10.8 mm/km on 19% of cells) against the upland's +3.5 ± 6.6, and elevation correlates
with easting at −0.623 so any easting term is confounded by construction. Then
`SWATH_TIE_INTERCEPT.md` independently: applying the new ties moves dE by 0.38 σ and dN by
0.18 σ, stable-ground NMAD 47.4 → 47.5 mm (flat), and the constants come out *flatter* W–E
(43.75 → 38.63 mm spread), i.e. they remove slightly LESS easting gradient. My hypothesis that
dE was residual across-swath structure is wrong.

What remains is not a tilt: a **patchy ±30 mm field at 0.5–1.5 km** on the upland whose linear
projection in northing is −17.9 ± 3.9 mm/km. Origin undecided between gen1, gen2 and real
ground change. Not the next job.

### Where DoD improvement now stands — decide after compaction

Ranked by what is measured, not guessed:
1. **Adopt the intercept ties** — built, tested, ~0.98 mm RMS agreement between extents. Costs
   a full product rebuild.
2. **Settle the absolute level** — `ac7b3810faa4e77f5` is resolving whether gen1 is +22.7 mm
   low or whether its own 2008 control (1 mm agreement at the well-sited `L1O101`) says
   otherwise. The re-gauge decision waits on this.
3. **The cross line** (`ab11056dffbd1d64d`) — first individually-identified per-line
   coefficients, three redundancy checks the network has never had.
4. **The canopy term is already shipped** — `q2 = 0.5 − 0.19·cover`, forest−open +23.4 → −1.1 mm.
5. **The patchy field** is the residual floor after all of the above.

## Agents — RUNNING at compaction (resume with SendMessage, ids are exact)

| id | doing | writes to |
|---|---|---|
| `ab11056dffbd1d64d` | **highest value.** CSF `4342-28-64` (cross line 10010 has zero class-2), then fit the three cross-line pairs to get c_136/c_137/c_138 INDIVIDUALLY, solve the network for c_135/134/133 from existing pair sums, and report the three redundancy residuals | `analysis/CROSS_LINE_FIT.md` |
| `ac7b3810faa4e77f5` | **gates the headline.** Resolve the sign tension: gen1's own 2008 control (public, MnGeo county validation reports, GEOID03, ~1,033 pts) vs our +22.7 mm anchor. Pin the published table's sign convention; compute our own residuals from the cloud; verdict on whether `z_before_absolute.npy` needs correcting; draft the MnGeo bias-adjustment request | `analysis/GEN1_OWN_CONTROL_TIE.md` |
| `a98d4163f0a93eeca` | extent-invariant pairwise ties (fitted intercept at `dtan = 0` instead of overlap mean) + stop gauging on the edge-cut swath; measure effect on the 8.0/9.8/17.4 mm disagreement and on dE/dN | `analysis/SWATH_TIE_INTERCEPT.md` |
| `ab885251acbb39916` | mine the lidar documentation sets (both control sets were found in REPORTS, not deliverables): the 2008 bias-adjustment value, flight plans/cross lines, swath-adjustment method both epochs, sensor/processing detail | `analysis/LIDAR_DOCUMENTATION_MINE.md` |

## Agents COMPLETED this session — their output is on disk, do not re-run

`a8831934b5349b50e` → `analysis/STABLE_POINT_TILT_AUDIT.md` (the tilt audit; dE is landform, dN is a patchy field) ·
`a6237d16819696743` → `analysis/SWATH_ACROSS_TRACK_TEST.md` (across-track term, no shared coefficient) ·
`af25167ecb43226e6` → `analysis/SWATH_DEGENERACY_BREAKING.md` (cross line 10010; tilt invisible to differences) ·
`a6576b9d9b60af45a` → `analysis/ADDITIONAL_GROUND_CONTROL.md` (534 2021 points; 2008 control IS public; siting is the constraint) ·
`a6affca7633cbd94c` → `analysis/ABSOLUTE_BASIS_ELBA.md` · `a14ad43c2986ff6b0` → `src/lidar_diff_icp/groundtruth/` ·
`a47b21616382075d6` → `analysis/MISSION_TIME_DRIFT.md` · `a9a64e947f04355c6` + `ae36408c3e8e87078` → `analysis/HIDDEN_FILTERS_AUDIT.md` ·
`a170d3d62a3105e2d` → `SESSION_AUDIT_AND_ENFORCEMENT.md` · `a7de9bb8b8d6e5709` → `TRUST_SYSTEM.md` + `trust/` ·
`a3d46fd0adca2674f` → `analysis/ELBAEXT2_SCOPE.md` · literature agent → `analysis/ridgelines/{DTM_PERCENTILE,CSF_PARAMETER}_LITERATURE.md`

## What was established today (results WITH their method)

**Absolute datum, new.** gen1 is **+22.7 mm low**, σ_total 39.7 mm, from two surveyed 3DEP
marks reached by swath chains: 2210 +21.3 ± 12.4 (west, 5 links, 128→133) and 2036 +28.9 ± 27.0
(east, 6 links, 144→138). They agree to 7.5 mm, but that is a consistency check, not the
accuracy — and they arrive on DIFFERENT swaths, comparable only through elbaext's 133↔138 link,
the worst-constrained one. Product `data/derived/elba_fulldensity/z_before_absolute.npy` with a
sidecar. Method + sign conventions in `docs/groundtruth.md`; `tie_mm = surveyed − z_lidar`, so
positive = lidar low. Module `src/lidar_diff_icp/groundtruth/`, 154 tests pass.

**gen2 against its own six marks** (never run before, no chain needed — same datum): NVA median
+17.2 mm low. So gen2 is not systematically low, the gen1 constant is gen1's own, and gen2 really
is on GEOID18. **But the four NVA marks span 103 mm, 40.8 mm RMS** — two fail 3DEP's own 35 mm
RMSEz on radius spread alone. **The mark and its terrain set the floor, not the lidar**; the
monuments sit on local highs (at 2210 the surveyed height is the p95 of gen1 returns within 5 m).

**Do NOT shift the DoD by the tie difference.** Correcting both epochs moves the DoD by only
22.7 − 17.2 = 5.5 mm, but paired at the same marks the epoch residual is +22.0 (2210) and −30.5
(2036) — opposite signs. The divides give gen2 − gen1 = −4.62 ± 1.93 mm on 24,487 cells, 20×
more precise and opposite in sign to the marks. Marks pin the LEVEL; divides pin the EPOCH
DIFFERENCE. Applying −5.5 would move the stable-divide DoD from −4.62 to −10.1.

**Canopy correction, shipped.** `q2 = 0.5 − 0.19·cover` — take gen2's ground at that percentile
of its own class-2 near-ground column instead of the median. `analysis/ridgelines/Q2_COVER_RELATION.md`;
product `dod_cover_q2.npy` + `.json`, control `dod_gen2_median.npy`, `gen2_q2_used.npy`. Removes
the false forest aggradation: forest−open +23.4 → −1.1 mm, residual ≤5 mm at every cover level
0–0.60, open untouched. Survived the filter audit and improved (b −0.1949 → −0.1887, linear
χ²/dof 1.13 → 0.73). q2 lives on gen2's GROUND-CLASSIFIED returns, where 0.50 reproduces the
vendor surface exactly — not the all-return column, where the vendor ground sits at rank 0.391.

## NEGATIVES — do not re-walk

- **Classifier choice does not matter.** gen1 CSF vs vendor TerraScan: 0.0 mm in every cover
  stratum. gen2 re-classified with our CSF: +1.1 to +5.6 mm, wrong sign to help.
- **CSF parameters do not matter for the surface.** rigidness 1 vs 3: −0.0 to −0.2 mm. threshold
  1.5 → 0.5: −3 mm, WORSE. `filters.outlier` on: 0.0 mm. rigidness 2 / cloth 2.5: slightly worse.
- **The percentile route is closed for the general problem.** Both epochs' ground columns are
  symmetric at every cover level (|Bowley skew| ≤ 0.048), so the offset is a pure translation and
  no order statistic recovers it. Width saturates while need grows 20×.
- **Mission-time drift: rejected.** Line ordinal beats gps_time at 6 of 7 sites; gaps go the wrong
  way (elba's 6,290 s gap gives +11.2 mm, its 1,022 s gap +23.9); Battle Creek's 19.7-hour break
  gives its smallest step; rate spans +46.9 to −74.7 mm/h and reverses sign. **Keep the per-swath
  constants as they are** — neither a time nor an ordinal model beats them against a 12.4 mm
  repeatability bar. `analysis/MISSION_TIME_DRIFT.md`.
- **Aircraft orientation: no evidence.** Headings alternate strictly S/N but dz ramps monotonically;
  heading term +1.3 mm at 0.46σ. Weak test though — n = 5 swaths, one site, because only elba and
  elbaext have a `beam_offset_table.parquet`. 42 more swaths across 10 sites could test it.
- **Re-referencing the swath solution cannot help.** `align_swaths` solves the free network and
  subtracts the reference afterwards (`coreg.py:485`), so re-referencing shifts every constant by
  one number and changes no difference (verified to 3.5e−15 mm).
- **Incidence is not a driver, under canopy either.** Within-cell slope of offset on incidence goes
  +4.4 → +0.3 mm/deg at every cover level once corrected. A POOLED cross-cell table cannot separate
  a beam effect from a cell-selection effect — always run the within-cell version.

## Process state — today's other half

~80% of the day went to Andy correcting my errors. `SESSION_AUDIT_AND_ENFORCEMENT.md` (705 lines)
catalogues it; `TRUST_SYSTEM.md` + `trust/` hold a designed enforcement system (Stop-hook gate,
provenance module, ledger), **written but NOT enabled**. `analysis/HIDDEN_FILTERS_AUDIT.md` found
149 filter sites in 63 files, 19 behind live results; the damaging ones are fixed (15 commits) and
several conclusions moved — see that file's FIXED section.

Standing rules now in `~/.claude/CLAUDE.md` (six provenance rules) and `HOW-I-FAIL.md` in memory,
which consolidated 12 deleted behavioural files. Memory 69 → 58 files.

**Two pending settings edits, both Andy's to make:**
1. `~/.claude/settings.json` PreToolUse hook emits `{"decision":"allow"}` — a dead schema that threw
   493 validation errors this session, and its BLOCK branch is the same dead shape, so the
   `gh release create` guard is likely not guarding. Corrected script written and tested at
   `~/.claude/hooks/block-release.sh`; the edit is to replace the inline command with that path.
2. Whether to enable the trust gate (`trust/settings.hooks.example.json` → `.claude/settings.local.json`).

## Reproduce

    env -u PROJ_DATA -u GDAL_DATA ./lidar-icp/bin/python analysis/ridgelines/q2_cover_fit.py --weight cells
    env -u PROJ_DATA -u GDAL_DATA ./lidar-icp/bin/python analysis/ridgelines/dod_cover_corrected.py
    env -u PROJ_DATA -u GDAL_DATA ./lidar-icp/bin/python analysis/groundtruth/elba_absolute_tie.py
    ./lidar-icp/bin/python -m pytest tests/ -q          # 154 pass

The venv `./lidar-icp/bin/python` has xdem; the conda `lidar-icp` env has PDAL but not xdem.
Unset `PROJ_DATA`/`GDAL_DATA` for anything touching rasterio/pyproj.

## Guardrails

Shared laptop: one heavy job at a time, watch `free -h`, prefer COPC + overlap polygons over whole
tiles. Never push/tag/release/close without explicit current-message authorisation. Floodplain mask
on ALL hillslope mass-balance work (today's `V_acc` runs were done WITHOUT it and need redoing).
Use `elba_fulldensity`, never the older decimated `data/derived/elba`.

## ★ CROSS LINE FOUND — psid 10010 (added at session end)

`analysis/SWATH_DEGENERACY_BREAKING.md`. **`point_source_id` 10010, heading 270.98° (due
west), 747,107 returns, in `data/before/4342-28-64.laz` — already on disk.** Speed 84.6 m/s,
half-width 719 m, gps_time 238,319.99–238,344.42 s of week, easting span 578,000–579,960,
northing 4,888,888–4,889,709, scan_angle_rank −17…+2. It sits between lines 138 and 137 in the
same night's monotonic progression, so it is the same sortie. All 18 other lines (128–146) are
N–S at headings 177.68–180.09° / 358.23–0.40°.

It crosses **136, 137, 138** over 55.2 / 110.3 / 57.3 ha, with corr(tan θ_A, tan θ_B) of
−0.056 / −0.154 / −0.052 against −0.92 to −0.96 on every N–S pair, and the identifiability
ratio SE(q)/SE(p) falling to 1.06 / 1.14 / 1.05. Line 137 is seen over its full range,
−0.287 to +0.287.

**Why it matters.** The degeneracy is NOT merely an alternating ±v — it is a **global
cross-track tilt of the mosaic**; an alternating `c_s` and a linear ramp in the constants are
the same object. Proven synthetically: under a 10 mm/km tilt the largest change in ANY pair
observation is **0.000e+00 mm**, because a tilt is identical for both lines at a shared point
and cancels in every between-line difference. So no overlap, loop, or cross line can see it.
**Two terms, two instruments: the cross line fixes the body-fixed per-line coefficients;
GROUND CONTROL fixes the tilt.** The two anchors already bound the tilt near zero
(UNVERIFIED, from that document: g = −0.485 ± 1.916 mm/km, anchors 15.49 km apart in easting).

**Cost to use it: a CSF pass.** Line 10010 carries **zero vendor class-2 returns** — all
745,942 are class 12 — so classification is the entire cost. No download.

Also settled there: **no second-neighbour overlap exists anywhere** (spacing 848–1060 m against
a swath sum of 1382–1476 m), and it would not help if it did.
